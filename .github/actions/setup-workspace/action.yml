name: Setup workspace
description: Shared tool setup, dependency install, and caches for Cortex web CI jobs

runs:
  using: composite
  steps:
    - name: Setup Node
      uses: actions/setup-node@v6
      with:
        node-version-file: package.json
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm ci
